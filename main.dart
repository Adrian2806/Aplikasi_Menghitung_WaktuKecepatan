import 'package:flutter/material.dart';
import 'kecepatan.dart';

void main() => runApp(
    MaterialApp(
      theme: ThemeData.light(
        //primarySwatch: Colors.red,
      ),
      home:  InputAPK(),
    )
);